# This folder is for local static assets (e.g. custom illustrations, logos)
# imported directly in components, for example: import logo from '@/assets/logo.svg'
#
# Product photography is NOT stored here — it lives in Supabase Storage
# (the 'product-images' and 'avatars' buckets) and is referenced by URL.
